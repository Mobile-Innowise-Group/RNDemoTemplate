module.exports = {
  // Use single quotes instead of double quotes for strings
  singleQuote: true,
  // Add spaces inside object literal braces
  bracketSpacing: true,
  // Place closing brackets on a new line for JSX/HTML tags
  bracketSameLine: false,
  // Don't add trailing commas (at the end of objects, arrays, etc.)
  trailingComma: "none",
  // Avoid parentheses around single arrow function parameters
  arrowParens: "avoid",
  // Always add semicolons at the end of statements
  semi: true,
  // Set the number of spaces per indentation level (4 spaces)
  tabWidth: 4,
  // Use tabs instead of spaces for indentation
  useTabs: true,
  // Set the maximum line length to 120 characters before wrapping
  printWidth: 120
}

